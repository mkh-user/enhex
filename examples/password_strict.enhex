# expect: ^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)[a-zA-Z\d]{8,}$
start
+ followed_by(zero_or_more(anything) + uppercase)
+ followed_by(zero_or_more(anything) + lowercase)
+ followed_by(zero_or_more(anything) + digit)
+ at_least(8, letter | digit)
+ end
